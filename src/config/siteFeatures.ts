/**
 * Section and CTA flags — adjust per branch when promoting to live (`temp-active`).
 *
 * Live (`temp-active`): work + about on; testimonials off; card CTA off until case studies are ready.
 * Dev (`main`): set `showWorkCardCta` to true when links are ready.
 */
export const SHOW_WORK_SECTION = true
export const SHOW_ABOUT_SECTION = true
export const SHOW_TESTIMONIALS_SECTION = false
export const SHOW_WORK_CARD_CTA = false
